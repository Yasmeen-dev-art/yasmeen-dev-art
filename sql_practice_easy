--Topic covered: Focus: Single-table queries, simple filtering, and ordering

--1. Retrieve the username first_name, and email of all authors who joined before June 1, 2023.
SELECT username, first_name, email
FROM Authors
WHERE join_date < '2023-06-01';

--2. Find the title and publication_date of all posts that are currently not published .
select title, publication_Date
from posts
where is_published is false;

--3. List all unique tag_name values in alphabetical order.
select distinct tag_name
from tags
order by tag_name;

--4. Find the comment_text and comment_date for the post titled 'The Joy of SQL Joins: A Beginner's Guide'.
SELECT c.comment_text, c.comment_date
FROM Comments c
JOIN Posts p 
  ON c.post_id = p.post_id -- Link comments to posts
WHERE p.title = 'The Joy of SQL Joins: A Beginner''s Guide'; -- Filter by the title

--5. --Show the username and email for the author with author_id = 2.
select username, email 
from authors
where author_id=2;

--COMMON MISTAKES:
/* 1. join_date < 2023-06-01 without single quotes throw error it will consider as 2023 minus 06 minus 01 as operation
so making it in single quotes mark it as string literal

2.  The Joy of SQL Joins: A Beginner''s Guide in this beginner''s is considered as escape sequence 

know the difference between single and double quotes
Single Quotes ('...') are used for String Literals (data values).

Example: 'Hello World', '2023-11-01', 'The Joy of SQL Joins: A Beginner''s Guide'

Double Quotes ("...") are used for Identifiers (table names, column names) that contain special characters and is case-sensitive.

Example: "Sales Table", "Author ID"*/

